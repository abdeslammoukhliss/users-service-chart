apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "users-services.fullname" . }}-test-connection"
  labels:
    {{- include "users-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "users-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
